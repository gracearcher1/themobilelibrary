# Copy this file to .env and fill in the values from your Supabase project
# (Project Settings -> Data API). Both are safe to expose in the frontend bundle --
# the anon key only grants what the Row Level Security policies in PLAN.md allow.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
